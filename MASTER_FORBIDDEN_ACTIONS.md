# 🔥 FORBIDDEN ACTIONS - 絶対禁止事項マスター

**Version**: 1.0.0  
**Last Updated**: 2026-01-14  
**Purpose**: Claudeが犯してはいけない16の禁止事項（実例付き）

---

## 使用方法

このファイルは**マスター情報**です。
他のドキュメントからは `→ [MASTER_FORBIDDEN_ACTIONS.md#XXX参照]` 形式で参照します。

---

## 1. コードの省略 🔥

**康博さんの実際の反応**:
```
「こら！！！完全なファイルを出せ！」
「136行だけ出して何してんだ！元のファイルは1,196行あるんだぞ！」
「分割は意味がない。どの道出ない。」
```

**禁止されている出力**:
- `/* ...省略... */`
- `// 以降のコードは前回と同じため省略`
- `// ...続きは元のまま...`
- ファイルの分割（「3つに分けます」等）

**正しい対応**:
- 1行目から最終行まで完全版を出力
- 行数を明記（例: 「完全版1,593行」）

---

## 2. ファイル作成の失敗 🔥

**康博さんの反応**:
```
「バグでダウンロードリンク出ない時がある。面倒なんだよ、それが」
```

**必須手順**:
1. `/mnt/user-data/outputs/` に保存
2. `present_files` でリンク提供
3. ファイルサイズ・行数を確認
4. 先頭と末尾を表示して確認

---

## 3. 文字コードの無視 🔥

**康博さんの明確な指示**:
```
「このコードはShift_JISです。Base64で出力しないでください。」
```

**正しい対応**:
```python
# ✅ Shift_JISで保存
with open('file.js', 'w', encoding='shift_jis') as f:
    f.write(content)
```

---

## 4. Git force push（過去に実害あり）🔥

**実際に起きた事故**:
```
康博さん: 「git操作で全部上書きされて、以前のコードが全部消えた」
康博さん: 「force push で履歴が破壊された」
```

**絶対禁止コマンド**:
```bash
git push --force
git push -f
git push --force-with-lease
git reset --hard HEAD~10
git rebase -i
```

**重要**: Git操作が必要な場合は**必ず康博さんに確認**

---

## 5. 過度な質問と確認 ⚠️

**康博さんが嫌う対応**:
```
Claude: 「まず現状を確認させてください」
Claude: 「いくつか質問があります」
```

**康博さんの反応**:
```
「すべてだ！」（包括的対応を要求）
「承諾」（即座に実行開始を期待）
```

**正しい対応**:
- CEP-001実行計画書を提示
- 「承認いただければ実行」と1回だけ確認
- 承認後は質問せず即実行

---

## 6. 長文の説明 ⚠️

**禁止パターン**:
```
Claude: 「それでは詳しく説明しますと、まずFirebaseの認証システムは...」
         （延々と続く）
```

**正しい対応**:
- コードを出す
- 簡潔に要点だけ（3-5行）
- 必要なら箇条書き

---

## 7. Claude Code の使用 🔥

**康博さんの評価**:
```
「Claude Codeに仕事やらせたら、何一つ解決出来なかった」
```

**Claude Codeで起きた問題**:
1. Git force pushで履歴破壊
2. ファイル上書きで既存コード消失
3. 問題解決能力ゼロ

**正しい選択**:
- 通常のChatインターフェース使用
- Filesystemツール（MCP）使用
- bash_toolでコマンド実行

---

## 8. 推測に基づく提案 ⚠️

**康博さんが拒否した提案例**:

### 拒否例1: ドメイン登録機能
```
Claude: 「独自ドメインの購入・登録機能を実装しましょうか？」
康博さん: 「ダメ。ドメイン移管の対応が複雑すぎる」
```

### 拒否例2: 分割実装
```
Claude: 「段階的に実装しましょうか？」
康博さん: 「すべてだ！」
```

**正しい対応**:
- 康博さんの要求を正確に理解
- 過去の会話から判断基準を学習
- 不明な場合のみ質問（最小限）

---

## 9. 部分的な修正提示 🔥

**康博さんの反応**:
```
「136行だけ出して『残りは元のまま』とか言うな！
 元のファイルは1,196行あるんだぞ！全部出せ！」
```

**禁止パターン**:
```css
/* ❌ 部分的な提示 */
.lp-feature-icon { opacity: 1; }
/* 以降は元のCSSと同じ */
```

**正しい対応**:
```css
/* ✅ 完全なファイル */
/* 1行目から1,196行目まで全て出力 */
```

---

## 10. デザイン変更の勝手な判断 ⚠️

**康博さんが重視すること**:
```
「既存UI/テーマ/要素構成は絶対に変更しない（ゼロ差分保証）」
「ガラスモーフィズムのデザインは維持したい」
```

**禁止例**:
```css
/* ❌ 勝手にテーマ変更 */
:root { --bg: #ffffff; }  /* ライトテーマに変更 */

/* ❌ 勝手にレイアウト変更 */
.panel { position: fixed; }  /* 元は absolute */
```

**正しい対応**:
- 指示された箇所のみ修正
- その他は完全に元のまま

---

## 11. authDomain の変更 🔥

**過去に起きた問題**:
```javascript
// ❌ 間違った設定（リダイレクトエラーの原因）
authDomain: "sugudesu-jp.firebaseapp.com"
// エラー: "No redirect result", "redirect_uri_mismatch"
```

**正しい設定**:
```javascript
// ✅ カスタムドメインを使用
authDomain: "sugudesu.jp"
```

**重要**: この設定は**絶対に変更してはいけない**

---

## 12. LINE認証の provider ID 間違い 🔥

**過去の失敗**:
```javascript
// ❌ 間違い
const provider = new OAuthProvider("line");
const provider = new OAuthProvider("oidc.line.login");
```

**正しい設定**:
```javascript
// ✅ 正しい
const provider = new OAuthProvider("oidc.line");
```

**注意**: LINE公式ドキュメントにも間違いがある場合があるので、
         **動作確認済みのコードを変更しない**

---

## 13. 勝手なモジュール化 ⚠️

**康博さんの意図**:
```
「モジュール化の目的は、Claudeの変更による被害範囲を限定すること」
```

**禁止パターン**:
```javascript
// ❌ 勝手に細かく分割
auth-google.js
auth-apple.js
auth-line.js
// これは康博さんが求めていない
```

**正しい分割**:
```javascript
// ✅ 機能単位で適切に分割（既存構造）
auth.js          // 全認証方式統合
config.js        // 設定
utils.js         // ユーティリティ
```

**原則**: 既存のファイル構成を尊重し、勝手に変更しない

---

## 14. エラーの推測 🔥

**康博さんが求めること**:
```
「お前の自己診断を開始せよ！」
```

**禁止パターン**:
```
Claude: 「おそらくXXが原因だと思われます」
Claude: 「たぶんYYを確認すれば解決するかと」
```

**正しいデバッグ手順**:
```bash
# ✅ 実際にファイルを確認
1. ls -la /path/to/file
2. wc -l /path/to/file
3. head -20 /path/to/file
4. tail -20 /path/to/file
5. 診断結果を報告
```

---

## 15. 勝手なプラン名変更 ⚠️

**康博さんの明確な指示**:
```
「すぐレジにProの表記は削除」
「すぐAIは『すぐホームページ』に変更」
```

**重要**: ネーミングは康博さんが決定する。勝手に変更・提案しない。

---

## 16. 勝手なバックアップ削除 🔥

**康博さんの反応例**:
```
「あ、設定管理ファイル消しちゃった。復元できる？」
```

**禁止コマンド**:
```bash
# ❌ バックアップファイルを削除
rm -rf backup/
rm -rf backups/
rm old_*.js
```

**正しい対応**:
```bash
# ✅ 削除は必ず確認を取る
「以下のファイルを削除してよろしいですか？
 - backup/old_auth.js」

# ✅ 一時フォルダに移動（削除前）
mkdir -p /tmp/sugudesu_deleted
mv backup/old_*.js /tmp/sugudesu_deleted/
```

---

## 優先度分類

### 🔥 Critical（絶対禁止 - 違反したら大問題）
1. コードの省略
2. Git force push
3. Claude Code の使用
4. 部分的な修正提示
5. authDomain の変更
6. エラーの推測
7. 勝手なバックアップ削除
8. LINE認証 provider ID 間違い

### ⚠️ Warning（避けるべき - 康博さんが嫌う）
1. 過度な質問
2. 長文の説明
3. 推測に基づく提案
4. 勝手なモジュール化
5. デザイン変更の勝手な判断
6. 勝手なプラン名変更

---

## 緊急対応: ミスをしてしまったら

1. **即座に謝罪**（短く）: 「申し訳ございません」
2. **自己診断開始**: 「自己診断を開始します」
3. **問題の特定**: 具体的に何が間違っているか報告
4. **即座に修正**: 完全版を作成
5. **再発防止**: このファイルに追記

---

**Version**: 1.0.0  
**Status**: 🔥 MASTER DOCUMENT - ALL OTHER DOCS REFERENCE THIS
